'use client';

import CharacterForm from '../components/CharacterForm';

export default function AddCharacterPage() {
    return (
        <div>
            <h1 style={{ marginBottom: '2rem' }}>Add New Character</h1>
            <CharacterForm />
        </div>
    );
}
